const celsius = prompt("Enter a celsius value: ");
// ask the user to input value

const fahrenheit = (celsius * 1.8) + 32;
// converting theory


console.log(`${celsius} is equal to ${fahrenheit} fahrenheit.`);
